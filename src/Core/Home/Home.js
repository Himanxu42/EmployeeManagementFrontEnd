import React from 'react';
import Headers from '../Headers/Headers'
import PropTypes from 'prop-types';

const Home = () => {
    return (
        <div>
          <Headers/>
        </div>
    );
};

Home.propTypes = {};

export default Home;